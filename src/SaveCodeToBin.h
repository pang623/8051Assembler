#ifndef SAVECODETOBIN_H
#define SAVECODETOBIN_H

#include <stdint.h>
#include <stdlib.h>
#include <stdio.h>

void saveCodeToBin(char *filename, uint8_t *codeMemory, int length);

#endif // SAVECODETOBIN_H
